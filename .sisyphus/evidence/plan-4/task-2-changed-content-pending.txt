Task 2 changed-content pending evidence
Scope: pending/send lifecycle mutation path

Implementation evidence:
- `BridgeStateManager.get_or_create` now returns `GetOrCreateResult(record, is_new, content_changed)` instead of raising on same-`bridge_message_id` / different-`content_hash`.
- Pending-lifecycle records can now mutate `content_markdown` and `content_hash` through `update_status` before continuing the existing send path.
- `BridgeOrchestrator.process_fixture` now routes changed content on `new` / `card_created` / `send_pending` and explicit-rejection `send_failed` through record mutation plus create/send or send-existing-card reuse.

Focused test evidence:
- `tests/test_bridge_orchestration.py::test_changed_content_pending_record_mutates_and_sends_existing_card`
- Focused run: `.venv/bin/python -m pytest tests/test_state.py tests/test_bridge_orchestration.py -q`
- Result: `38 passed in 0.76s`

Full-suite check:
- `.venv/bin/python -m pytest -q`
- Result: `76 passed in 1.77s`
